{
  "name": "onclass-lme-jidouka-app",
  "private": true,
  "engines": { "node": "18.x" },
  "dependencies": { "playwright": "1.55.0" },
  "scripts": {
    "heroku-postbuild": "npx playwright install chromium && npx playwright install-deps chromium"
  }
}
