YouTube 連携の Google 認証（OAuth）が失効し、YouTube 系のバッチが停止しています。

検知日時: <%= @detected_at.strftime("%Y年%-m月%-d日 %H時%M分") %>
エラー: <%= @reason %>

下記URLを開き、YouTubeチャンネルの管理権限があるGoogleアカウントで再認証してください。

<%= @authorize_url %>

「認証成功！」と表示されれば復旧です。次回のスケジュール実行から自動で再開します。

--
このメールは失効を検知するたびに1通だけ送信されます（再認証が完了すると次回分が解禁されます）。
